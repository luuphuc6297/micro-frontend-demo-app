import { BaseTheme } from '@microfrontend-demo/design-system/styles';

export const Theme = {
    ...BaseTheme,
    colors: {
        ...BaseTheme.colors,
        tds: '#009688',
        tds_dark: '#00796B',
        tds_light: '#009688'
    }
};

export default Theme;
